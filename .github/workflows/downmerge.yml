name: Downmerge Changes
on:
  push:
    branches:
      - main

jobs:
  Sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo 🛎️
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Downmerge main -> dependabot-updates ⛙
        run: |
          git checkout dependabot-updates
          git merge -m 'Merge branch 'main' into dependabot-updates' main
          git push origin dependabot-updates

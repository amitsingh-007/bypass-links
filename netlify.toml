[dev]
  port = 8888

[build]
  publish = "client/build"
  functions = "server/routes/"
  targetPort = 5000
  environment = { NODE_VERSION = "12" }

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
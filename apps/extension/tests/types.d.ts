/**
 * Chrome extension API global available in extension page/service worker contexts.
 * Used in Playwright evaluate() calls where the code runs in the browser context.
 */

declare const chrome: any;
